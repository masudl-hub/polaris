[pytest]
testpaths = tests
asyncio_mode = auto
timeout = 120
addopts = -v --tb=short --strict-markers
markers =
    unit: Unit tests (no model loading, fast)
    integration: Integration tests (loads ML models, slower)
    slow: Tests that take > 10s (e.g. pytrends calls)
